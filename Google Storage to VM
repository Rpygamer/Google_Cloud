!gsutil cp gs://bucket_name/file_name  file_name
Exemple: 
!gsutil cp gs://kaggle-201621/test.csv.zip test.csv.zip 

from VM to google storage
curl https://sdk.cloud.google.com | bash
exec -l $SHELL
gcloud init
gsutil cp file1 file2 gs://bucket



gsutil config -b

Then surf to the URL it provides, [ CLICK Allow ]

Then copy the verification code and paste to terminal.
